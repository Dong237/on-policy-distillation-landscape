Memo: None of the assigned WP2 black-box/API candidates clears strict OPD. GAD and PRISM have the strongest C1/C2 evidence because they score current policy rollouts with adaptive discriminators, but their policy objectives consume scalar reward or advantage signals, so they remain below strict. OVD has same-rollout verbal scoring, yet converts it into rejection/mixed GRPO training rather than direct dense distillation. SODA and ORPO-Distill are preference-distillation neighbors, but their student traces are static or mixed-policy negatives rather than exact current rollouts receiving teacher supervision. Secondary spot-check rows were not added; this file stays limited to the assigned queue.

| method | source_url | exact_section | C1 quote | C2 quote | C3 quote | teacher_access | supervision_granularity | objective_family | rollout_freshness | final_label | reason_to_downgrade | confidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GAD | https://arxiv.org/abs/2511.10643 | Sec. 2.1 "GAD: Generative Adversarial Distillation"; Sec. 2.2 "Training"; Algorithm 1; Appendix A.1 | "Sample student responses G(x)" | "score the teacher response higher than the student's output" | "treat D(G(x)) as a reward" | black-box teacher text outputs plus learned discriminator | sequence-level scalar discriminator score on prompt-response pairs | adversarial discriminator reward with GRPO / policy gradient | current_policy online rollouts | partial_opd | C1 and C2 are present, but C3 is consumed as response-level reward/GRPO, not non-scalar teacher distribution matching. | high |
| PRISM | https://arxiv.org/abs/2604.28123 | Sec. 3.2 "Distribution Alignment via On-Policy Distillation"; Sec. 3.2.4 "Adversarial On-Policy Distillation"; Appendix D Algorithm 1 | "sampled from the current policy" | "higher score to the reference and a lower score to the policy rollout" | "updating the policy via GRPO" | black-box Gemini-derived reference pool plus MoE discriminator | response-level MoE discriminator scores for perception and reasoning components | adversarial distribution alignment with Bradley-Terry discriminator and GRPO | current_policy online rollouts | borderline_strict | Response-level adversarial supervision is stronger than static preference fitting, but the policy update uses discriminator reward/advantages rather than dense teacher-style supervision. | high |
| OVD | https://arxiv.org/abs/2601.21968 | Sec. 3 "On-policy Verbal Distillation"; "On-policy Sampling"; "Verbal Rejection Sampling"; "Verbelized Distillation" | "student model generates multi-step reasoning trajectories" | "teacher provides quality assessments as discrete scores" | "used to construct training samples for GRPO" | black-box teacher, reasoning agent, or environment agent giving verbal scores | step-level and trajectory-level verbal scores, usually 0-9 | verbal rejection sampling plus GRPO / policy gradient | current_policy rollouts with teacher replacement for rejected trajectories | partial_opd | Same-rollout verbal scoring exists, but the objective is GRPO over accepted/replaced trajectories; low-score rollouts are not directly distilled with dense teacher supervision. | high |
| SODA | https://arxiv.org/abs/2604.03873 | Sec. 2.2 "SODA: Semi On-Policy Distillation with Alignment"; Algorithm 1 | "sample responses from the base student q0 before any fine-tuning" | "pair them against the teacher responses" | "apply Direct Preference Optimization (DPO)" | black-box teacher text responses | sequence-level preference pair: teacher preferred, base-student response rejected | static DPO preference distillation | static_base_student_snapshot | partial_opd | Fails strict freshness: the negative signal is a one-time q0 snapshot, not current policy rollouts; C3 is static DPO preference fitting. | high |
| ORPO-Distill | https://arxiv.org/abs/2509.25100 | Sec. 2.1 "Odds Ratio Preference Optimization"; Sec. 2.2 "Preference Dataset Creation"; Sec. 2.3 "Mixed-Policy Update" | "new negative trace set sampled after every epoch" | "Chosen is a teacher CoT trace" | "contrastive ORPO loss objective" | black-box teacher-generated traces with student-generated negatives | sequence-level preference triplets: teacher chosen, student rejected | ORPO contrastive preference optimization | mixed_policy base-student plus latest-checkpoint negatives | partial_opd | Mixed-policy negatives include student traces, but the teacher does not supervise the exact current rollout; C3 is ORPO preference optimization, not strict OPD. | high |
